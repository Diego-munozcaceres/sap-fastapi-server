version: "3.8"

services:
  sap-backend:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: sap-fastapi-server
    ports:
      - "8000:8000"
    env_file:
      - .env
    restart: unless-stopped
